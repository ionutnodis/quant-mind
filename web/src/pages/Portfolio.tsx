import { Panel } from "../components/Panel";

export function Portfolio() {
  return (
    <Panel title="Portfolio" note="in build">
      <p className="text-muted">This bench is being built.</p>
    </Panel>
  );
}
